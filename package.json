{
  "name": "character-sheet-viewer",
  "version": "1.0.0",
  "description": "Google Apps Script Character Sheet Viewer",
  "scripts": {
    "build": "node build.js",
    "lint": "eslint server/ client/src/",
    "lint:fix": "eslint server/ client/src/ --fix",
    "lint:server": "eslint server/",
    "lint:client": "eslint client/src/",
    "test": "jest",
    "clean": "node -e \"const fs = require('fs'); if(fs.existsSync('dist')) fs.rmSync('dist', {recursive: true, force: true})\"",
    "deploy": "npm run build && cd dist && clasp push && cd .. && npm run clean"
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.6",
    "eslint": "^8.57.0",
    "jest": "^30.2.0"
  }
}