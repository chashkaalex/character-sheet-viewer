//const { Character } = require('../character');

/**
 * Parses the known spells structure from list items
 * @param {Character} character - The character to parse
 * @returns {ParsedSpellsStructure}
 */
// function ParseKnownSpells(character) {

// }
